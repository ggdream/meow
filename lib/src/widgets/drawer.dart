import 'package:flutter/material.dart';

class MeowGlobalDrawer extends StatelessWidget {
  const MeowGlobalDrawer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Drawer(
      child: Column(
        
      ),
    );
  }
}
